use w3schools;

select * from Customers where CustomerName like '% %';

select * from Products;

select AVG(price) from Products where CategoryID = 3;
select AVG(price) from Products;
select count(price) from Products where CategoryID = 3;
select sum(price) from Products where CategoryID = 3;
select * from Products where CategoryID = 3;
